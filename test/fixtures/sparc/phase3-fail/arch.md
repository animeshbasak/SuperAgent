# Architecture

We'll use the standard pattern. Plain prose, no signatures, no constraint mapping.

The toggle reads from somewhere and writes back. Probably localStorage but maybe a cookie. We'll figure it out during implementation.
